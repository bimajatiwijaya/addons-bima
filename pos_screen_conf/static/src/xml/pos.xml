<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="Product">
        <span class='product' t-att-data-product-id="product.id">
            <div class="product-img">
                <img t-att-src='image_url' />
                <t t-if="widget.pos.config.price_display">
                    <t t-if="!product.to_weight">
                        <span class="price-tag">
                            <t t-esc="widget.format_currency(product.price,'Product Price')"/>
                        </span>
                    </t>
                    <t t-if="product.to_weight">
                        <span class="price-tag">
                            <t t-esc="widget.format_currency(product.price,'Product Price')+'/'+widget.pos.units_by_id[product.uom_id[0]].name"/>
                        </span>
                    </t>
                </t>

            </div>
            <div class="product-name">
                <t t-esc="product.display_name"/>
            </div>
        </span>
    </t>

    <t t-name="Orderline">
        <li t-attf-class="orderline #{ line.selected ? 'selected' : '' }">
            <span class="product-name">
                <t t-esc="line.get_product().display_name"/>
                <t t-if="line.get_product().tracking!=='none'">
                    <i t-attf-class="oe_link_icon fa fa-list oe_icon line-lot-icon #{line.has_valid_product_lot() ? 'oe_green' : 'oe_red' }"/>
                </t>
            </span>
            <span class="price">
                <t t-if="widget.pos.config.price_display">
                    <t t-esc="widget.format_currency(line.get_display_price())"/>
                </t>
            </span>
            <ul class="info-list">
                <t t-if="line.get_quantity_str() !== '1' || line.selected ">
                    <li class="info">
                        <em>
                            <t t-esc="line.get_quantity_str()" />
                        </em>
                        <t t-esc="line.get_unit().name" />
                        <t t-if="widget.pos.config.price_display">
                            at
                            <t t-esc="widget.format_currency(line.get_unit_display_price(),'Product Price')" />
                            /
                            <t t-esc="line.get_unit().name" />
                        </t>
                    </li>
                </t>
                <t t-if="line.get_discount_str() !== '0'">
                    <li class="info">
                        With a
                        <em>
                            <t t-esc="line.get_discount_str()" />%
                        </em>
                        discount
                    </li>
                </t>
            </ul>
        </li>
    </t>

    <t t-name="OrderWidget">
        <div class="order-container">
            <div class="order-scroller touch-scrollable">
                <div class="order">
                    <t t-if="orderlines.length === 0" >
                        <div class='order-empty'>
                            <i class='fa fa-shopping-cart' />
                            <h1>Your shopping cart is empty</h1>
                        </div>
                    </t>
                    <t t-if="orderlines.length > 0">
                        <ul class="orderlines"></ul>
                        <div class="summary clearfix">
                            <div class="line">
                                <t t-if="widget.pos.config.price_display">
                                    <div class='entry total'>
                                        <span class="label">Total: </span> <span class="value">0.00 €</span>
                                        <div class='subentry'>Taxes: <span class="value">0.00€</span></div>
                                    </div>
                                </t>
                                <t t-if="widget.pos.config.price_display != true">
                                    <div class='entry total oe_invisible'>
                                        <span class="label oe_invisible">Total: </span> <span class="value oe_invisible">0.00 €</span>
                                        <div class='subentry oe_invisible'>Taxes: <span class="value oe_invisible">0.00€</span></div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </t>
                </div>
            </div>
        </div>
    </t>
</templates>
